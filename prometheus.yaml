global:
  scrape_interval: 5s # Как часто собирать метрики

scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/actuator/prometheus' # Путь к метрикам в Java
    static_configs:
      - targets: ['java-app:8086'] # Имя твоего контейнера и ВНУТРЕННИЙ порт